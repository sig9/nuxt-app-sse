{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "build:cloudflare": "NITRO_PRESET=cloudflare nuxt build",
    "build:cloudflare:win": "set NITRO_PRESET=cloudflare&nuxt build",
    "mf:preview": "miniflare .output/server/index.mjs --site .output/public",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.6",
    "wrangler": "^2.0.24",
    "miniflare": "^2.6.0"
  }
}
