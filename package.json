{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "fb:build": "NITRO_PRESET=firebase yarn build",
    "fb:build:win": "set NITRO_PRESET=firebase&yarn build",
    "fb:emu:start": "firebase emulators:start",
    "fb:deploy": "firebase --debug deploy",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "firebase-admin": "^11.0.1",
    "firebase-functions": "^3.22.0",
    "firebase-functions-test": "^2.2.0",
    "firebase-tools": "^11.4.0",
    "nuxt": "3.0.0-rc.6"
  }
}
